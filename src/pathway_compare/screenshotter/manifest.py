"""Manifest helpers for recording capture metadata and provenance."""


def build_capture_manifest(source_path: str, captured_files: list[str]):
    """Create a placeholder manifest describing captured artifacts."""
    pass
